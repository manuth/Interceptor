import Assert = require("assert");

suite(
    "Interceptor",
    () =>
    {
        test(
            "Example…",
            () =>
            {
                Assert.strictEqual(1, 1);
            });
    });
